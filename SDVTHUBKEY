-- 🌱 Plant Vs Brainrot | SDVT Hub (Rayfield + Flexible Key System)
-- Key hợp lệ: "SDVTSCRIPT" HOẶC bất kỳ chuỗi 12 chữ số nào

-- ⚡ Load Rayfield
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- Key Window
local KeyWindow = Rayfield:CreateWindow({
    Name = "🔑 SDVT Hub Key System",
    LoadingTitle = "Enter Key",
    LoadingSubtitle = "Key Required",
    ConfigurationSaving = { Enabled = false }
})

local KeyTab = KeyWindow:CreateTab("Key", "lock")

local enteredKey = ""

KeyTab:CreateInput({
    Name = "Enter Key",
    PlaceholderText = "Type your key here...",
    RemoveTextAfterFocusLost = false,
    Callback = function(text)
        enteredKey = text
    end
})

KeyTab:CreateButton({
    Name = "Submit Key",
    Callback = function()
        -- ✅ Kiểm tra: "SDVTSCRIPT" hoặc 12 chữ số
        local isValid = (enteredKey == "SDVTSCRIPT") or 
                        (tonumber(enteredKey) ~= nil and #enteredKey == 12 and enteredKey:match("^%d+$"))

        if isValid then
            warn("✅ Correct Key!")
            
            -- ⚡ Flash effect
            local PlayerGui = game:GetService("Players").LocalPlayer:WaitForChild("PlayerGui")
            local flashGui = Instance.new("ScreenGui")
            flashGui.IgnoreGuiInset = true
            flashGui.ResetOnSpawn = false
            flashGui.Parent = PlayerGui

            local flashFrame = Instance.new("Frame")
            flashFrame.Size = UDim2.new(1, 0, 1, 0)
            flashFrame.BackgroundColor3 = Color3.new(1, 1, 1)
            flashFrame.BackgroundTransparency = 1
            flashFrame.Parent = flashGui

            task.spawn(function()
                for i = 1, 6 do
                    flashFrame.BackgroundTransparency = 0
                    task.wait(0.25)
                    flashFrame.BackgroundTransparency = 1
                    task.wait(0.25)
                end
                flashGui:Destroy()

                -- ✅ Load main script
                local success, err = pcall(function()
                    loadstring(game:HttpGet("https://raw.githubusercontent.com/Shadow-Vertex/SDVTHUB/refs/heads/main/SDVTHUB"))()
                end)
                if not success then
                    Rayfield:Notify({
                        Title = "Error",
                        Content = "Failed to load main script.",
                        Duration = 5,
                        Image = 4483362458
                    })
                end
            end)
        else
            warn("❌ Invalid Key! Must be 'SDVTSCRIPT' or 12 digits.")
            Rayfield:Notify({
                Title = "Invalid Key",
                Content = "Key phải là 'SDVTSCRIPT' hoặc gồm đúng 12 chữ số.",
                Duration = 4,
                Image = 4483362458
            })
        end
    end
})

-- 🔑 Nút "Get Key" → mở trang web
KeyTab:CreateButton({
    Name = "🌐 Get Key from Web",
    Callback = function()
        local url = "https://bao760.github.io/hairobloxweb11/SDVTHUBKEY.html"
        setclipboard(url)
        Rayfield:Notify({
            Title = "🔗 Get Key",
            Content = "Link đã được sao chép! Hoàn thành nhiệm vụ trên web để nhận key 12 số.",
            Duration = 6,
            Image = 4483362458
        })
    end
})
